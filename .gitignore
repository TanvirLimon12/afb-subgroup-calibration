# Corpus and trained weights are not distributed with the code.
# Leading slashes anchor these to the repository root so they cannot match
# afb_calibration/data/ or results/runs/.
/Datasets/
/data/
/runs/
*.pt
*.pth
*.zip

# build / env
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
/build/
/dist/

# os
.DS_Store
